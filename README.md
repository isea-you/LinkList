# LinkList
基于java实现的链表（含虚拟头结点）

链表这种数组结构相比于数组，真正的实现了动态的存储，试想一下，数组只有在数组的末尾添加元素才会非常的快速O(1)，如果在数组的中间（index位置）添加元素的话，数组中的元素就得一个一个的往后面挪，这是O(n)，但是链表只需要变换一下指针的指向即可。

同时，链表也因此丧失了随机访问的能力。

本次实现的链表将会包含虚拟头结点，还支持泛型，因此在这个链表中可以存储任意类型的同一种数据类型

